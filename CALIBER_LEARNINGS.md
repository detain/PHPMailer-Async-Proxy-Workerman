# Caliber Learnings

Accumulated patterns and anti-patterns from development sessions.
Auto-managed by [caliber](https://github.com/caliber-ai-org/ai-setup) — do not edit manually.

- **[env]** `GITHUB_TOKEN` is set in this shell to an INVALID token — `gh` falls back to keyring login (account `detain`) only if `GITHUB_TOKEN` is unset for the call. Wrap every `gh` invocation as `unset GITHUB_TOKEN; gh ...` inline, otherwise `gh` will error with "The token in GITHUB_TOKEN is invalid".
- **[gotcha]** This fork lives NESTED inside the `mailbaby-mail-api` parent repo at `/home/sites/mailbaby-mail-api/PHPMailer-Async-Proxy-Workerman/`. Always `cd` into the fork (or use absolute paths) before running `git`/`composer` — running them from the parent operates on the wrong repo. Set `git config user.email` / `user.name` **locally inside the fork** (no `--global`), otherwise the parent repo's identity leaks in.
- **[convention]** Author identity for commits in this fork: `Joe Huss <detain@interserver.net>` (canonical — user types it inconsistently like "intertserver"; always normalize to `interserver.net`).
- **[pattern]** After editing `composer.json`, validate with `php -r 'json_decode(file_get_contents("composer.json"), false, 512, JSON_THROW_ON_ERROR);' && composer validate --no-check-all` before committing — catches JSON syntax + composer-schema errors in one shot.
- **[gotcha]** Pin `phpunit/phpunit` at `^9.6` ONLY, not `^9.6 || ^10.5`. `phpunit.xml.dist` uses the legacy PHPUnit 8.5 schema (`<listeners>`, `<filter>/<whitelist>`, `convertDeprecationsToExceptions`) and `test/DebugLogTestListener.php` `implements PHPUnit\Framework\TestListener` — PHPUnit 10 removed that interface, so the listener silently breaks. Same applies to `yoast/phpunit-polyfills`: stay on `^1.1`, not `^2.0` (which targets PHPUnit 10+).
- **[gotcha]** There is no committed `composer.lock`. Partial-update commands like `composer update phpunit/phpunit --with-all-dependencies` error with `Cannot update only a partial set of packages without a lock file present`. Run plain `composer update` (or `composer install` for fresh setups).
- **[gotcha]** Expect ~71 baseline test errors all reading `Test config params missing - copy testbootstrap-dist.php to testbootstrap.php`. They come from `SendTestCase::set_up()` and exist on master too — `test/testbootstrap.php` is gitignored. To eliminate, `cp test/testbootstrap-dist.php test/testbootstrap.php` and edit creds; otherwise treat that error count as the green baseline, not regression.
- **[pattern]** To prove failing tests are pre-existing (not caused by your branch): `git stash && git checkout master && composer install --no-interaction && vendor/bin/phpunit --no-coverage 2>&1 | tail -5` to capture baseline error count, then `git checkout <branch> && git stash pop`. Compare error totals — if equal/lower on your branch, the failures aren't yours.
- **[pattern]** Multi-step PR cadence in this fork: `git commit` → `git push -u origin <branch>` → `unset GITHUB_TOKEN; gh pr create --base master --head <branch> --title ... --body "$(cat <<'EOF' ... EOF)"` → `unset GITHUB_TOKEN; gh pr merge <PR#> --squash --delete-branch` → `git checkout master && git pull --rebase`. PR bodies use HEREDOC with backtick-escaped code spans (`\`name\``) so the shell does not interpret them.
- **[gotcha]** There is NO `composer.lock` and it is intentional — `composer.json` has `"config": { "lock": false }` (plus `composer.lock` is in `.gitignore`). Partial-update commands like `composer update phpunit/phpunit --with-all-dependencies` error with `Cannot update only a partial set of packages without a lock file present`. Run plain `composer update` (or `composer install` for fresh setups), and never check in a lockfile.
- **[gotcha]** Expect ~71 baseline test errors all reading `Test config params missing - copy testbootstrap-dist.php to testbootstrap.php`. They come from `SendTestCase::set_up()` and exist on master too — `test/testbootstrap.php` is gitignored. To eliminate, `cp test/testbootstrap-dist.php test/testbootstrap.php` and edit creds; otherwise treat that error count as the green baseline, not regression. After adding new passing tests the count above 71 stays exactly the same (e.g. 670→692 total, errors still 71).
- **[pattern]** When `phpcs` reports style errors marked `[x]` ("PHPCBF CAN FIX … AUTOMATICALLY"), run `vendor/bin/phpcbf <path>` to auto-fix, then re-run `phpcs` to confirm clean — don't hand-edit fixable issues. Warnings without `[x]` (e.g. "Line exceeds 120 characters") must be fixed manually; split long lines (e.g. extract long literal IPv6 strings into `$src`/`$dst` locals before passing to constructors).
- **[convention]** phpcs in this project enforces "first expression of a multi-line control structure must be on the line after the opening parenthesis" — multi-line `if`/`while` conditions must break to a new line immediately after `(`, not have the first operand sit beside the paren.
- **[convention]** New source under `src/` should start with `declare(strict_types=1);` after the docblock and use a `final` class where inheritance isn't part of the contract — matches the fork's existing additions (`src/Async/*`, `src/ProxyProtocol/*`). Exceptions thrown from a sub-namespace (`PHPMailer\PHPMailer\ProxyProtocol\Exception`) should `extends \PHPMailer\PHPMailer\Exception` so existing top-level catchers keep working.
- **[gotcha]** Expect ~71 baseline test errors all reading `Test config params missing - copy testbootstrap-dist.php to testbootstrap.php`. They come from `SendTestCase::set_up()` and exist on master too — `test/testbootstrap.php` is gitignored. To eliminate, `cp test/testbootstrap-dist.php test/testbootstrap.php` and edit creds; otherwise treat that error count as the green baseline, not regression. After adding new passing tests the count above 71 stays exactly the same (e.g. 670→692→697→703 total, errors still 71).
- **[gotcha]** You cannot pass a `protected` method as `[$this, 'methodName']` to a strictly-typed `?callable` parameter declared in another namespace — PHP's strict-callable check rejects it with `TypeError: Argument #1 ($handler) must be of type ?callable, array given`. This bit `SMTP::setTransport()` when handing `[$this, 'errorHandler']` (protected) to `Transport::setErrorHandler(?callable)`. Fix: wrap in a `\Closure` bound to `$this`, e.g. a `buildErrorHandlerClosure()` helper that returns `function (...) use (&$this) { $this->errorHandler(...); }`. The array-callable form only works for `public` methods or when the caller is in the same class.
- **[pattern]** For an in-process SMTP integration test, fork a TCP listener with `pcntl_fork`: child runs `stream_socket_server('tcp://127.0.0.1:0', ...)` (port 0 picks ephemeral), writes a `220 mock smtp ready\r\n` banner so the parent's `SMTP::connect()` can complete, then drains bytes into a tempfile until EOF/timeout; parent runs the real `SMTP` against `127.0.0.1:$port`; `pcntl_waitpid` after. `set_up()` should `markTestSkipped` when `pcntl_fork` isn't available. See `test/ProxyProtocol/SmtpIntegrationTest.php`.
- **[pattern]** When wiring new async deps (revolt/event-loop, Fibers), keep tests runnable under plain PHPUnit without an outer reactor — pattern is `FiberRunner::run(Closure)` that branches on `Fiber::getCurrent()` and `EventLoop::getDriver()->isRunning()`: inline inside fiber, suspend on existing reactor, or spin a private `$driver->run()` for CLI/PHPUnit. See `src/Async/FiberRunner.php` for the canonical three-mode implementation.
- **[gotcha]** Expect ~71 baseline test errors all reading `Test config params missing - copy testbootstrap-dist.php to testbootstrap.php`. They come from `SendTestCase::set_up()` and exist on master too — `test/testbootstrap.php` is gitignored. To eliminate, `cp test/testbootstrap-dist.php test/testbootstrap.php` and edit creds; otherwise treat that error count as the green baseline, not regression. After adding new passing tests the count above 71 stays exactly the same (e.g. 670→692→697→703→706→709 total, errors still 71).
- **[pattern]** When `phpcs` reports style errors marked `[x]` ("PHPCBF CAN FIX … AUTOMATICALLY"), run `vendor/bin/phpcbf <path>` to auto-fix, then re-run `phpcs` to confirm clean — don't hand-edit fixable issues. Warnings without `[x]` (e.g. "Line exceeds 120 characters") must be fixed manually; split long lines by extracting locals (e.g. `$delay = $remaining > 0 ? $remaining : 0.001;` before passing to `EventLoop::delay($delay, ...)`) rather than wrapping the existing expression.
- **[pattern]** For an in-process SMTP integration test, fork a TCP listener with `pcntl_fork`: child runs `stream_socket_server('tcp://127.0.0.1:0', ...)` (port 0 picks ephemeral), writes a `220 mock smtp ready\r\n` banner so the parent's `SMTP::connect()` can complete, then drains bytes into a tempfile until EOF/timeout; parent runs the real `SMTP` against `127.0.0.1:$port`; `pcntl_waitpid` after. `set_up()` should `markTestSkipped` when `pcntl_fork` isn't available. See `test/ProxyProtocol/SmtpIntegrationTest.php` and `test/Async/MockSmtpServer.php`. For the async variant, **bind the listener in the parent first** (so you know the port before forking), then hand the listening resource into the child via `runChild($server)` — avoids the post-fork race where the parent connects before the child's `stream_socket_server` returns.
- **[gotcha]** `stream_socket_enable_crypto($sock, true, $method)` on a non-blocking stream returns THREE distinct values, not just bool: `true` = handshake complete, `false` = handshake failed (cert/protocol mismatch), and **integer `0` = would-block, kernel needs more I/O — caller MUST retry after the socket becomes readable OR writable**. The strict-equality check `=== 0` is required; `!$ok` would treat would-block as failure. See `WorkermanTransport::cryptoLoop()` for the suspension+retry pattern using both `EventLoop::onReadable` and `EventLoop::onWritable` watchers (TLS may need either direction during handshake).
- **[gotcha]** Before calling `enableCrypto()` on a stream that just spoke plaintext (STARTTLS path), drain any already-buffered server greeting/response with `readLine()` first. Bytes left in the userspace `$readBuffer` will sit AHEAD of the TLS ClientHello response and corrupt the handshake decode.
- **[gotcha]** Self-signed `tls://127.0.0.1:0` listener + `pcntl_fork` + `stream_socket_accept` is FLAKY as a positive-path TLS test — the child often exits with no accepted connection inside the timing window and the parent gets an empty transcript. Prefer testing the *would-block loop* itself: connect to a silent plain-TCP listener, call `enableCrypto()` with a 2s budget, and assert the call sat ~1.5–4s (proving it suspended on the event loop, not busy-looped) and returned false. Real TLS handshake success is best verified against a live SMTPS test server in `MailTransportTest`, not a fork-and-accept in-process fixture.
- **[pattern]** Driving `STREAM_CLIENT_ASYNC_CONNECT` to completion: after `stream_socket_client(..., STREAM_CLIENT_ASYNC_CONNECT)` returns a resource, suspend on `EventLoop::onWritable($sock, ...)` until it fires, then verify success with `socket_import_stream($sock)` + `socket_get_option($s, SOL_SOCKET, SO_ERROR)` — a non-zero SO_ERROR means the connect actually failed even though the socket is writable. See `WorkermanTransport::waitForConnected()`.
